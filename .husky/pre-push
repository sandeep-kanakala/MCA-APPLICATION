# Go to frontend folder (only if package.json is there)
cd ./frontend || exit 1

echo "🔹 Running lint..."
npm run lint || exit 1

echo "🔹 Running build..."
npm run build || exit 1

echo "✅ All pre-push checks passed. You can push now!"